import { Stack } from "expo-router";
import { Platform } from 'react-native';
export default function DynamicLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    >
   
    </Stack>
  );
}